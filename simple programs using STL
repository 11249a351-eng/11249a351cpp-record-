Aim
To write simple C++ programs that demonstrate basic usage of the Standard Template Library (STL) components such as vector, list, map, stack, and queue.

⭐ Program 1: Vector – Insertion & Display
Algorithm
Declare vector
Insert elements using push_back()
Display elements using iterator
End

Program
#include <iostream>
#include <vector>
using namespace std;
int main() {
    vector<int> v;

    v.push_back(10);
    v.push_back(20);
    v.push_back(30);

    cout << "Vector Elements: ";
    for (auto it = v.begin(); it != v.end(); ++it) {
        cout << *it << " ";
    }

    return 0;
}

Output
Vector Elements: 10 20 30

⭐ Program 2: List – Insertion & Deletion
Algorithm
Declare list
Insert elements
Delete an element
Display

Program
#include <iostream>
#include <list>
using namespace std;

int main() {
    list<int> myList = {10, 20, 30};

    myList.push_front(5);
    myList.pop_back();

    cout << "List Elements: ";
    for (int x : myList)
        cout << x << " ";

    return 0;
}

Output
List Elements: 5 10 20

⭐ Program 3: Map – Key–Value Pairs
Algorithm
Declare map
Insert key–value pairs
Display using iterator

Program
#include <iostream>
#include <map>
using namespace std;
int main() {
    map<int, string> m;

    m[1] = "Apple";
    m[2] = "Banana";
    m[3] = "Cherry";

    cout << "Map Elements:" << endl;
    for (auto p : m) {
        cout << p.first << " -> " << p.second << endl;
    }

    return 0;
}

Output
Map Elements:
1 -> Apple
2 -> Banana
3 -> Cherry

⭐ Program 4: Stack – Push & Pop
Algorithm
Declare stack
Push elements
Pop top element
Display top

Program
#include <iostream>
#include <stack>
using namespace std;
int main() {
    stack<int> s;

    s.push(10);
    s.push(20);
    s.push(30);

    s.pop();   // removes 30

    cout << "Top element = " << s.top();

    return 0;
}

Output
Top element = 20

⭐ Program 5: Queue – Enqueue & Dequeue
Algorithm
Declare queue
Add elements
Remove one element
Display front

Program
#include <iostream>
#include <queue>
using namespace std;

int main() {
    queue<int> q;

    q.push(100);
    q.push(200);
    q.push(300);

    q.pop();   // removes 100

    cout << "Front element = " << q.front();

    return 0;
}

Output
Front element = 200

Result
The programs successfully demonstrate simple operations using the STL:
Vector
List
Map
Stack
Queue
along with iterators and basic container functions.
